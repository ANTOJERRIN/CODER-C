// program to print all the characters btw 'a' to 'z'
#include <stdio.h>
int main() {
    /*char ch;
    for (ch = 'a'; ch <= 'z'; ch++) {
        printf("\n%c ", ch);
    }
    return 0;*/
    /*int c=97;
    for(;c<122;c++){
        printf("\n%c ",c);
    }
    return 0;8*/
    // wacp to print my name
     char name[30]="A JERRIN ANTO";
    printf("%s",name);
    // WACP TO PRINT THE CHARACTERS OF MY NAME ONE BY ONE
    /*char name[30]="A JERRIN ANTO";
    int i=0;
    for(;name[i]!='\0';i++)
        {
        printf("\n%c",name[i]);
    }
    return 0;*/
    // wacp to print consosnaunts in my name
    /*char name[30]="A JERRIN ANTO";
    int i=0;
    for(;name[i]!='\0';i++){
        if(name[i]=='A' || name[i]=='E' || name[i]=='I' || name[i]=='O' || name[i]=='U' || name[i]==' '){
            continue;
        }
        else{
            printf("\n%c",name[i]);
        }
    }   
    return 0;*/
    // wacp to print vowels in my name
    /*char name[30]="A JERRIN ANTO";    
    int i=0;
    for(;name[i]!='\0';i++){
        if(name[i]=='A' || name[i]=='E' || name[i]=='I' || name[i]=='O' || name[i]=='U'){
            printf("\n%c",name[i]);
        }
    }
    return 0;*/

}